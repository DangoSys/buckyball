name: Goban Check

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  checks: write

concurrency:
  group: buckyball-${{ github.workflow }}-${{ github.sha }}
  cancel-in-progress: false

jobs:
  check:
    uses: ./.github/workflows/chip-check.reusable.yml
    with:
      chip: goban
      verilator_config: sims.verilator.BuckyballGoban2CoreVerilatorConfig
      run_batch_tests: false
