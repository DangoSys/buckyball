# Reservation Station (RS) Module

Memory-domain reservation station types and `MemReservationStation` live here. Live ROB scheduling uses `GlobalROB` under `framework/frontend/globalrs`.

## Files

```
rs/
└── reservationStation.scala  - MemReservationStation and issue/commit bundles
```
